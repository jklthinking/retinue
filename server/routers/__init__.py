"""Domain routers for the Retinue server.

Each module groups the routes that serve one thing, and ``create_app`` in
``server.app`` composes them. Import order here is registration order; the
package itself holds no behaviour.
"""
